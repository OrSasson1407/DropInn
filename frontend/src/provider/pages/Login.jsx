import React, { useState } from 'react'; import { login } from '../../shared/services/auth'; import { useNavigate, Link } from 'react-router-dom'; import '../../shared/styles/auth.css'; export default function ProviderLogin() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const nav = useNavigate(); const handleLogin = async (e) => { e.preventDefault(); try { await login(email, password); nav('/provider'); } catch (err) { alert(err.message); } }; return <div className='auth-container'><h2>Provider Login</h2><form className='auth-form' onSubmit={handleLogin}><input className='auth-input' type='email' placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)} required /><input className='auth-input' type='password' placeholder='Password' value={password} onChange={e=>setPassword(e.target.value)} required /><button className='auth-btn' type='submit'>Login</button></form><Link to='/provider/signup'>Apply to be a Provider</Link></div>; }
