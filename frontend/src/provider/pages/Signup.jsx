import React, { useState } from 'react'; import { signup } from '../../shared/services/auth'; import { useNavigate, Link } from 'react-router-dom'; import '../../shared/styles/auth.css'; export default function ProviderSignup() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const nav = useNavigate(); const handleSignup = async (e) => { e.preventDefault(); try { await signup(email, password); nav('/provider'); } catch (err) { alert(err.message); } }; return <div className='auth-container'><h2>Provider Signup</h2><form className='auth-form' onSubmit={handleSignup}><input className='auth-input' type='email' placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)} required /><input className='auth-input' type='password' placeholder='Password' value={password} onChange={e=>setPassword(e.target.value)} required /><button className='auth-btn' type='submit'>Sign Up</button></form><Link to='/provider/login'>Already a Provider?</Link></div>; }
