import React, { useEffect, useState } from 'react'; import { db } from '../../firebase'; import { collection, query, where, onSnapshot } from 'firebase/firestore'; import { Link } from 'react-router-dom'; export default function Home() { const [providers, setProviders] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { const q = query(collection(db, 'providers'), where('isAvailable', '==', true)); return onSnapshot(q, (snap) => { setProviders(snap.docs.map(d => ({ id: d.id, ...d.data() }))); setLoading(false); }); }, []); return (<div style={{ padding: '20px', fontFamily: 'sans-serif' }}><h2>Available Now - Men's Haircuts</h2>{loading ? <p>Loading barbers...</p> : (<div style={{ display: 'grid', gap: '15px' }}>{providers.length === 0 ? <p>No barbers available right now.</p> : providers.map(p => (<div key={p.id} style={{ border: '1px solid #ddd', padding: '15px', borderRadius: '8px' }}><h3>{p.name || 'Barber'}</h3><p>Rating: {p.rating || 'New'} | Price: {p.price || 100} ILS</p><Link to={'/customer/provider/' + p.id}><button style={{ padding: '10px 15px', background: '#000', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' }}>View & Book</button></Link></div>))}</div>)}</div>); }
