import React, { useState, useEffect } from 'react'; import { useParams, useNavigate } from 'react-router-dom'; import { createOrder } from '../../shared/services/firestore'; import { useAuth } from '../../shared/context/AuthContext'; import { processPayment } from '../../shared/services/payments'; import { db } from '../../firebase'; import { doc, onSnapshot } from 'firebase/firestore'; export default function BookingFlow() { const { id: providerId } = useParams(); const { currentUser } = useAuth(); const nav = useNavigate(); const [address, setAddress] = useState(''); const [orderId, setOrderId] = useState(null); const [status, setStatus] = useState(''); useEffect(() => { if (!orderId) return; return onSnapshot(doc(db, 'orders', orderId), (docSnap) => { if(docSnap.exists()) setStatus(docSnap.data().status); }); }, [orderId]); const handleBook = async () => { if (!currentUser) return nav('/customer/login'); await processPayment(100, providerId); const ref = await createOrder(currentUser.uid, providerId, { address, price: 100 }); setOrderId(ref.id); }; if (orderId) { return <div style={{ padding: '20px' }}><h2>Booking Status: {status}</h2>{status === 'completed' && <div><h3>Leave a Review</h3><button onClick={()=>nav('/')} style={{ padding: '10px', background: '#000', color: '#fff' }}>Submit & Go Home</button></div>}</div>; } return (<div style={{ padding: '20px', maxWidth: '500px', margin: 'auto' }}><h2>Book Haircut</h2><div style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><input placeholder='Your Address' value={address} onChange={e=>setAddress(e.target.value)} style={{ padding: '10px' }} required /><p>Price: 100 ILS</p><button onClick={handleBook} style={{ padding: '15px', background: '#000', color: '#fff', border: 'none', cursor: 'pointer' }}>Pay & Book</button></div></div>); }
